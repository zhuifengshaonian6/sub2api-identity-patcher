{
  "name": "sub2api-identity-patcher",
  "version": "0.1.0",
  "private": true,
  "description": "Local Chrome/Edge extension for patching SUB2API exported account JSON identity fields.",
  "scripts": {
    "test": "node --check popup.js && node --check patcher-core.js && node tests/patcher-core.test.js"
  },
  "license": "MIT"
}
